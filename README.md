emutrix is a basic matrix-style mixer for EMU1010 based cards produced by Creative Labs. 


wiki: https://github.com/token0/emutrix/tree/wiki


building:

```
qmake "CONFIG+=debug" && make

```


to install:

```
sudo make install

```
